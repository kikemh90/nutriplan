# NutriPlan – Fase 4.2

Corrección del modal “Añadir al calendario”.

- Estado cerrado: `visibility:hidden`, `opacity:0` y `pointer-events:none`.
- Solo `.modal.open` hace visible e interactivo el pop-up.
- Se mantiene la animación al abrir/cerrar.
- Caché del service worker incrementada a `nutriplan-pwa-v1.2`.
